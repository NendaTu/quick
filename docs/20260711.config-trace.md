# config.py Variable Trace — 2026-07-11

## Summary of Config Variable Connection Status

| Variable Name | Status | Traced Component & Usage |
|---|---|---|
| `MODE` | Fully Connected & Working | Configures operational mode (`live`, `demo`, `paper`). Sourced inside constructors of `Engine`, `SignalRouter`, etc. |
| `BITGET_API_KEY` | Fully Connected & Working | Used by `BitgetExchange` to initialize the Live `data_client`. |
| `BITGET_SECRET_KEY` | Fully Connected & Working | Used by `BitgetExchange` to initialize the Live `data_client`. |
| `BITGET_PASSPHRASE` | Fully Connected & Working | Used by `BitgetExchange` to initialize the Live `data_client`. |
| `BITGET_API_KEY_DEMO` | Fully Connected & Working | Used by `BitgetExchange` to initialize private Demo execution clients. |
| `BITGET_SECRET_KEY_DEMO` | Fully Connected & Working | Used by `BitgetExchange` to initialize private Demo execution clients. |
| `BITGET_PASSPHRASE_DEMO` | Fully Connected & Working | Used by `BitgetExchange` to initialize private Demo execution clients. |
| `USE_VIRTUAL_BALANCE` | Fully Connected & Working | Checked in `BitgetExchange.get_trading_equity()` to decide if using starting virtual equity or actual exchange USDT balance. |
| `INITIAL_EQUITY` | Fully Connected & Working | Starting balance for simulation, paper mode, and virtual balance tracking. |
| `REINVESTMENT_PERCENTAGE` | Fully Connected & Working | Mustafa sweep strategies and LearningModel query this for compounding calculations. |
| `RISK_PER_TRADE` | Fully Connected & Working | Position sizing model and strategy entry calculations default to this percentage. |
| `MAX_CONCURRENT_POSITIONS` | Fully Connected & Working | Enforced inside `Engine._trading_loop()` to limit open/pending positions. |
| `DRAWDOWN_LIMIT` | Fully Connected & Working | Checked in `Engine._equity_monitor()` to protect total capital. |
| `TOTAL_ROI_LIMIT` | Fully Connected & Working | Checked in `Engine._equity_monitor()` for session profit targets. |
| `MAX_TRADES_LIMIT` | Fully Connected & Working | Checked in `Engine._equity_monitor()` to shut down after N trades. |
| `MAX_DURATION` | Fully Connected & Working | Sourced in `Engine._equity_monitor()` to limit runtime in seconds. |
| `ASSETS_COUNT` | Fully Connected & Working | Sourced in `Engine.start()` and `Simulator.initialize_metadata()` to size discovery list. |
| `ASSET_REDISCOVERY_HOURS` | Fully Connected & Working | Sourced in `Simulator.initialize_metadata()` to determine asset discovery cache age in database. |
| `ASSET_OMITTED` | Fully Connected & Working | Used in `Engine.start()` and `Simulator.initialize_metadata()` to filter out symbols (e.g. BTCUSDT). |
| `BTC_SYMBOL` | Fully Connected & Working | Used throughout engine core and exchanges as the primary confluence symbol. |
| `MAX_START_DATE` | Fully Connected & Working | Used by downloader and database historical queries. |
| `MAX_END_DATE` | Fully Connected & Working | Used by downloader and database historical queries. |
| `ACTIVE_TIMEFRAME` | Fully Connected & Working | Sourced in `Engine._trading_loop()` and `Simulator.get_features()` for TTL and features. |
| `AVAILABLE_TIMEFRAMES` | Fully Connected & Working | Checked by downloader and database warm-up layers. |
| `TF_SECONDS` | Fully Connected & Working | Used throughout the codebase for multi-timeframe candle operations. |
| `ENTRY_ORDER_TYPE` | Fully Connected & Working | Configures entry order execution (`limit` or `market`). |
| `TP_ORDER_TYPE` | Fully Connected & Working | Configures take-profit execution type. |
| `SL_ORDER_TYPE` | Fully Connected & Working | Configures stop-loss execution type. |
| `SL_DISASTER_BUFFER` | Fully Connected & Working | Checked in `Simulator._process_orders()` for soft stop disaster backup. |
| `SL_MOVE` | Fully Connected & Working | Read as fallback stop-loss percentage in entry calculations. |
| `TP_MOVE` | Fully Connected & Working | Read as fallback take-profit percentage in entry calculations. |
| `TARGET_NET_ROE` | Fully Connected & Working | Used as a target placeholder in the LearningModel. |
| `LIMIT_CHASE_TIMEOUT` | Fully Connected & Working | Used in `BitgetExchange._process_orders()` to cancel stale unfilled entries. |
| `USE_ONLINE_LEARNING` | Fully Connected & Working | Checked in `LearningModel.train_on_tick()` and `train_on_trade()` to toggle weights training. |
| `REENTRY_COOLDOWN` | Fully Connected & Working | Sourced in `Engine._asset_is_tradable()` for asset-level reentry lockout. |
| `MIN_SCALING_DISTANCE_PCT` | Fully Connected & Working | Sourced in `Engine._trading_loop()` to enforce minimum price distance before scaling into positions. |
| `MAX_ATR_EXPANSION_MULTIPLIER` | Fully Connected & Working | Sourced in `range_sweep_ATR.1.mustafa.py` to discard extreme parabolic exhaustion candles exceeding this cap. |
| `MIN_NET_TP_PROFIT_PCT` | Fully Connected & Working | Sourced in `Simulator.place_trade_oco()` and `BitgetExchange.place_order()` to reject narrow fee-trap entry trades. |
| `USE_TP_RELAXATION` | Fully Connected & Working | Checked in `LearningModel.predict()` to adapt targets during flat trends. |
| `TP_RELAXATION_THRESHOLD` | Fully Connected & Working | Used in `LearningModel.predict()` for trend boundaries. |
| `USE_TTL` | Fully Connected & Working | Checked in `Engine._trading_loop()` to trigger force-exits. |
| `TTL_CANDLE_MULTIPLIER` | Fully Connected & Working | Sourced in `Engine._trading_loop()` to scale TTL limit. |
| `USE_BREAKEVEN_TRIGGER` | Fully Connected & Working | Sourced in `Simulator._process_orders()` for stop-loss movement. |
| `BREAKEVEN_ROI_THRESHOLD` | Fully Connected & Working | Used in `Simulator._process_orders()` for trigger point. |
| `BREAKEVEN_PROFIT_BUFFER` | Fully Connected & Working | Used in `LearningModel.predict()` and `Simulator._process_orders()` for padding. |
| `EXIT_STRATEGY` | Fully Connected & Working | Configures take-profit mode (`BE+TP` or `BE+TP1+TP2`). |
| `TP1_QTY_RATIO` | Fully Connected & Working | Used to size partial take-profits. |
| `TP1_BUFFER_PCT` | Fully Connected & Working | Used to calculate TP1 targets. |
| `CONTRARIAN_GLOBAL` | Fully Connected & Working | Used to flip order direction globally. |
| `CONTRARIAN_FILTER` | Fully Connected & Working | Used to invert logic on entry gates. |
| `USE_DRT_VELOCITY` | Fully Connected & Working | Used in `LearningModel.predict()` to check momentum acceleration. |
| `USE_ADAPTIVE_RSI` | Fully Connected & Working | Sourced in `LearningModel.predict()` for adaptive bands. |
| `USE_ATR_CAPPED_TP` | Fully Connected & Working | Sourced in `LearningModel.predict()` for take-profit ceilings. |
| `USE_VOL_ADJUSTED_RISK` | Fully Connected & Working | Sourced in `LearningModel.predict()` to reduce sizing during high volatility. |
| `RESTRICT_BTC_MOMENTUM` | Fully Connected & Working | Sourced in `LearningModel.predict()` as a momentum gate. |
| `BTC_MOMENTUM_THRESHOLD` | Fully Connected & Working | Checked in `LearningModel.predict()` for momentum boundaries. |
| `BYPASS_GLOBAL_FILTERS` | Fully Connected & Working | Sourced in `Engine._asset_is_tradable()` and `Engine._trading_loop()` to skip Layer 1 filters. |
| `RESTRICT_BTC_CONFLUENCE` | Fully Connected & Working | Sourced in `LearningModel.predict()` to gate trades based on BTC direction. |
| `RESTRICT_ASSET_CONFLUENCE` | Fully Connected & Working | Sourced in `LearningModel.predict()` for asset trend alignment. |
| `BTC_CONF_15M_MIN` | Fully Connected & Working | Sourced in `LearningModel.predict()` for minimum BTC 15m confluence. |
| `BTC_CONF_1H_MIN` | Fully Connected & Working | Sourced in `LearningModel.predict()` for minimum BTC 1H confluence. |
| `TREND_15M_MIN` | Fully Connected & Working | Used in `LearningModel.predict()` for scoring trend strength. |
| `RESTRICT_DRT` | Fully Connected & Working | Gated in `LearningModel.predict()` to filter out weak trends. |
| `RESTRICT_CONFIDENCE` | Fully Connected & Working | Gated in `LearningModel.predict()` for confidence floors. |
| `RESTRICT_DIRECTIONAL_SANITY` | Fully Connected & Working | Gated in `LearningModel.predict()` to match scoring with trend direction. |
| `RESTRICT_RSI` | Fully Connected & Working | Gated in `LearningModel.predict()` for RSI oversold/overbought boundaries. |
| `RESTRICT_RSI_SHORT_CEILING` | Fully Connected & Working | Gated in `LearningModel.predict()` for shorting limits. |
| `RESTRICT_MACD` | Fully Connected & Working | Gated in `LearningModel.predict()` for MACD histogram. |
| `RESTRICT_15M_TREND` | Fully Connected & Working | Gated in `LearningModel.predict()` for 15m trend alignment. |
| `RESTRICT_SUPERTREND` | Fully Connected & Working | Gated in `LearningModel.predict()` for Supertrend alignment. |
| `RESTRICT_ATR` | Fully Connected & Working | Gated in `LearningModel.predict()` for minimum ATR volatility. |
| `RESTRICT_VOL_PCT` | Fully Connected & Working | Gated in `LearningModel.predict()` for minimum volume threshold. |
| `RESTRICT_SPREAD` | Fully Connected & Working | Gated in `LearningModel.predict()` to filter wide spreads. |
| `RESTRICT_LIQUIDITY` | Fully Connected & Working | Gated in `Engine._asset_is_tradable()` for minimum order book depth. |
| `RESTRICT_SLIPPAGE` | Fully Connected & Working | Checked in `Simulator.place_trade_oco()` and `Simulator._process_orders()`. |
| `RESTRICT_SCORE` | Fully Connected & Working | Gated in `LearningModel.predict()` for model scoring limits. |
| `MIN_CONFIDENCE` | Fully Connected & Working | Gated in `LearningModel.predict()` as the confidence threshold. |
| `RESTRICT_IMBALANCE` | Fully Connected & Working | Gated in `LearningModel.predict()` for minimum book imbalance. |
| `RESTRICT_HTF_BIAS` | Fully Connected & Working | Gated in `LearningModel.predict()` for Higher Timeframe trend bias. |
| `RESTRICT_VOLUME_INFLUX` | Fully Connected & Working | Gated in `LearningModel.predict()` for volume spikes. |
| `RESTRICT_MIN_VAL` | Fully Connected & Working | Checked in `Simulator.place_trade_oco()` for minimum contract values. |
| `RESTRICT_STRUCTURE` | Fully Connected & Working | Gated in `LearningModel.predict()` for BOS/MSS structure. |
| `LOG_REJECTIONS` | Fully Connected & Working | Checked in `Simulator.place_trade_oco()` for warning level logging. |
| `LOG_SIGNALS` | Fully Connected & Working | Checked in `Engine._trading_loop()` for console logging. |
| `SHOW_PERIODIC_SUMMARY` | Fully Connected & Working | Toggled in `Engine.start()` for summary tasks. |
| `SUMMARY_INTERVAL_SECONDS` | Fully Connected & Working | Sourced in `Engine._summary_task()` for loop timing. |
| `SHOW_HEARTBEAT` | Fully Connected & Working | Toggled in `Engine.start()` for heartbeat tasks. |
| `HEARTBEAT_INTERVAL_SECONDS` | Fully Connected & Working | Sourced in `Engine._heartbeat_task()` for heartbeat timing. |
| `TICK_SECONDS` | Fully Connected & Working | Controls ticks intervals (primarily simulation). |
| `INDICATOR_PRICE_HISTORY` | Fully Connected & Working | Sourced in `Simulator.get_features()` for slicing. |
| `MAKER_FEE` | Fully Connected & Working | Sourced in `Simulator._process_orders()`, `Simulator._execute_exit()`, and `tools/trading_utils.py` for fee accounting. |
| `TAKER_FEE` | Fully Connected & Working | Sourced in `Simulator._execute_entry_direct()` and `tools/trading_utils.py` for fee accounting. |
| `EXPECTED_SLIPPAGE` | Fully Connected & Working | Sourced in `LearningModel.predict()` and `tools/trading_utils.py` for slippage calculation. |
| `MAX_ENTRY_SLIPPAGE` | Fully Connected & Working | Sourced in `Simulator.place_trade_oco()` and `Simulator._process_orders()`. |
| `MAX_SPREAD_PCT` | Fully Connected & Working | Checked in `LearningModel.predict()`. |
| `FEE_AWARE_SIZING` | Fully Connected & Working | Sourced in `LearningModel.predict()` and `tools/trading_utils.py`. |
| `BTC_REALTIME_CONFLUENCE` | Fully Connected & Working | Checks if BTC confluence analysis is required in real-time. |
| `SESSION_MULTIPLIER` | Fully Connected & Working | Sourced in `LearningModel.predict()` to scale risk during London/NY sessions. |
