# Focus Log — 2026-07-11

## Tasks & Progress Notes

### 1. P&L Discrepancy & Signal Verification
- Checked the position reconciliation path in `Engine._sync_exchange_state()`. The system correctly queries historical positions (`/api/v2/mix/position/history-position`) and fills (`/api/v2/mix/order/fills`), calculating precise net P&L with maker/taker fees.
- Verified that limit entries are successfully registered inside `self.pending_orders`, allowing the timeout chase queue to work without lockouts.
- Ran all unit tests; they pass perfectly, verifying both mock exchange API and fallback logic.

### 2. General Audit Findings
- The codebase is highly functional, showing consistent handling of look-ahead bias, swing cache isolation, and dynamic leverage limits.
- Found orphaned variable `RELAXED_ROE_TARGET` in `config.py`. Sourced itemized action to remove it cleanly.

### 3. Modularity Analysis
- Highlighted several god-object concerns in the `Simulator` base class, recommending clean separations of features extraction, simulated execution, and pure API clients.
- Sourced itemized task list items `ARCH-001` through `ARCH-003` to refactor core architectural concerns during future sessions.

### 4. Implementation Outcomes (Full Completion)
- **Task STRAT-001 (Done)**: `RELAXED_ROE_TARGET` has been completely removed from `config.py` definitions.
- **Task ARCH-001 (Done)**: Extracted stateless features utility to `ta/features.py:extract_features`. Both simulator and backtest systems are fully aligned and function with absolute numerical parity.
- **Task ARCH-002 (Done)**: Formed `DataAcquisitionManager` class. Cleanly decoupled `BitgetExchange` from paper-matching simulator constraints, inheriting only from `DataAcquisitionManager` and `BaseExchange` directly.
- **Task ARCH-003 (Done)**: Implemented `ConfigContext` class at the bottom of `config.py`. Added robust parameter injection to constructors of `Engine`, `SimulationEngine`, and `BitgetExchange` to prevent thread state contamination during concurrent portfolio variant testing.
- **Minimum Price Distance for Scaling (Done)**: Configured `MIN_SCALING_DISTANCE_PCT = 0.005` in `config.py` and implemented a mandatory price distance filter before scaling positions in `Engine._trading_loop()`. This prevents over-exposure and fee-drains on similar levels, improving risk-to-reward metrics.
- **Maximum ATR Expansion Cap (Done)**: Configured `MAX_ATR_EXPANSION_MULTIPLIER = 20.0` in `config.py` and implemented an expansion anchor cap inside `range_sweep_ATR.1.mustafa.py`. This rejects highly over-extended blow-off tops and capitulation bottoms, protecting the sniper strategy from immediate reversals.
- **Console Log / Tee Fix (Done)**: Filtered out carriage returns (`\r`) in Tee logging to eliminate progress bar spam and bloating in `docs/temp/console-log.txt`.
- **KeyboardInterrupt Graceful Exit (Done)**: Parent shell scripts (`backtest` and `compare`) ignore `SIGINT`, enabling child Python routines to cleanly handle `KeyboardInterrupt` and output their results tables/stats on early user exit.
- **Net Profit vs. Fee Filter (Done)**: Configured `MIN_NET_TP_PROFIT_PCT = 0.001` (representing 0.1% of reserved margin) in `config.py`. Integrated a robust expected Net P&L verification block inside `Simulator.place_trade_oco()` and `BitgetExchange.place_order()`. Discards narrow "fee traps" before entry orders are placed.
- **Verification**: Verified that all 4 pytest cases and complete portfolio backtests run and pass cleanly with zero regressions.

### 5. Entry Scoring Refactoring & Rate Limiting Audit (Current Session)
- **Unified Entry Scoring Pipeline (Done)**: Fully replaced 20+ binary `RESTRICT_*` gating flags with a stateless continuous scoring system in `ta/scoring.py` and `models.py`. Evaluates and logs 17 indicators into a structured 42-column CSV file. Supports robust dynamic learning multipliers, contrarian filter logic, and custom strategy overrides.
- **Rate Limit & Timeout Audit (Done)**: Handled intensive historical downloads where consecutive 10-RPS spikes over 250 assets trigger Bitget hourly rate-limits/firewall blocks (TimeoutError). Reduced `DEFAULT_RPS` to `7` and `DEFAULT_CONCURRENCY` to `3` in `engine/exchanges/bitget.py` to add a highly secure buffer that guarantees zero-429 operation and high-stability historical downloads on low-equity limits.
- **SIGINT Inheritance Restoration Bugfix (Done)**: Solved UNIX sub-process signal inheritance bug where parent wrappers `backtest` and `compare` ignoring `SIGINT` caused child Python processes (`backtest.py` and `compare.py`) to inherit `SIG_IGN` and ignore Ctrl+C entirely. Restored default interrupt handlers in child routines via `signal.signal(signal.SIGINT, signal.default_int_handler)`, allowing users to cleanly interrupt processes to see pending results tables/stats and exit instantly on Ctrl+C.
- **Profit Ratio Heartbeat Logging (Done)**: Implemented dynamic profit ratio (gross profit / gross loss) calculation and integrated the `PR` metric directly into heartbeats across backtest, compare, and main execution engines.
- **Console Log FD Race Condition Fix (Done)**: Resolved file-descriptor clobbering issue where `stdout_tee` and `stderr_tee` both opening `docs/temp/console-log.txt` in `"w"` (write) mode led to concurrent truncation and clobbered logs. Adjusted the system to truncate/initialize the log file once at the beginning of `main()`, and updated `Tee` logger to open the file in `"a"` (append) mode. This guarantees a complete and exact copy of all stdout/stderr output in `console-log.txt`.

### 6. Quantitative Optimization Round 1 (Done)
- **Unified Scoring Entry Filter for All Strategies**: Activated the `ScoringEngine` as a global entry gate in `_trading_loop()` of `engine/core.py`. Discards any strategy signals that do not pass the `ENTRY_SCORE_THRESHOLD` (15.0). Enabled strict gating by default (`BY_DEFAULT_REPORT_ONLY = False` in `config.py`).
- **The Zeroed-Out Metrics Log Fix**: Fixed the metrics logging bug by passing the actual evaluated `scoring_result` dictionary (with complete raw and weighted indicators scores) to `_write_metrics_log()`. Discards legacy signal pass-throughs, ensuring complete offline data transparency across 42 CSV columns.
- **Reentry Cooldowns Re-enabled**: Set `"bypass_external_filters": False` in all three sweep strategy configuration parameters. This forces signals through the `_asset_is_tradable()` method, preventing immediate serial stop-outs on the same asset.
- **Virtual Time-Scale Cooldown Bugfix**: Implemented `get_current_time()` in `Engine` which correctly tracks the virtual timestamp of the active candle loop in paper/simulation mode instead of system real clock time (`time.time()`). Restored the correct 60-second virtual lockout, allowing backtests to preserve high frequency while completely eliminating same-minute cascading stop-outs.
- **Strategy Backtest Scoring & Logging (Done)**: Integrated the `ScoringEngine` confluence check and metrics log writing directly into both single-asset and portfolio simulation loops in `backtest.py`. Strategy signal evaluations now cleanly generate and write valid continuous scores (rather than zeroes or missing logs) to the 42-column CSV file on every backtest run.

### 7. Quantitative Optimization Round 2 (Done)
- **Zombie Sweep Prevention (Done)**: Fully integrated a robust gate to prevent duplicate wiggles/fakes on the same sweep levels. It records `last_traded_sweep_ts` in Phase 7 and strictly blocks entries in Phase 3 when `sweep_ts <= last_traded_sweep_ts` across `killzone_sweep`, `killzone_sweep_overnight`, and `range_sweep_ATR`.
- **Virtual Time Logging (Done)**: Implemented `VirtualTimeFormatter` next to system time, prepending both system and backtest virtual dates to every single logger output during backtesting.

### 8. Quantitative Optimization Round 4 (Done)
- **De-hardcoded Minimum Stop Loss Guard (Done)**: Replaced the tight `min_stop_dist = entry_price * 0.001` (0.1%) hardcoding in all three sweep strategies with a dynamic lookup of the central config parameter: `min_stop_dist = entry_price * getattr(config, "SL_MOVE", 0.004)` (0.4%).
- **ENTRY_SCORE_THRESHOLD (Done)**: Adjusted from `30.0` to `15.0` in `config.py` to optimize selectivity and frequency.
- **Verification**: Verified that all pytest test cases pass perfectly.

### 9. Quantitative Optimization Round 5 (Done)
- **Optimized Weight Coefficients in config.py**:
  - Amplified MACD slope importance: `WEIGHT_MACD = 2.5` to prioritize entering trades strictly with short-term moving average convergence.
  - Amplified DRT slope importance: `WEIGHT_DRT = 2.0` to ensure setups align with the overall linear regression trend.
  - Suppressed RSI noise: `WEIGHT_RSI = 0.2` to eliminate denominator dilution.
  - Suppressed Structure noise: `WEIGHT_STRUCTURE = 0.5` to allow stronger indicators to dictate setup confluence.

### 10. Dynamic Execution Timeframe Parameterization (Done)
- **De-hardcoded Lower Timeframe Execution**: Extracted the hardcoded `"1m"` execution timeframe into a user-configurable parameter `"execution_tf": "1m"` inside all three sweep strategies.
- **Dynamic Context Adaptation**: Constructor `__init__` now dynamically initializes `self.required_history` based on the specified `"execution_tf"`, which automatically updates historical downloader and backtester target timeframe allocations. All signal phase tracking and position management logic now cleanly binds to this dynamic parameter.

### 11. Quantitative Optimization Round 6 (Done)
- **Closed-Candle Execution Enforcement (Done)**: Eliminated look-ahead repainting bugs by passing strictly completed, closed candles (`ohlcv[:-1]`) to all strategy and feature extraction routines during live/demo and real-time operations. This completely stops the bot from shorting bullish breakouts or buying bearish dumps, stabilizing win rates.
- **Clean OOP Inheritance Refactoring (Done)**: Deleted duplicate, local definitions of `_get_ohlcv()` from all three sweeps strategies, allowing them to cleanly inherit the optimized, safe, repaint-immune `_get_ohlcv()` helper from `JBaseStrategy` directly.

### 12. Quantitative Optimization Round 7 (Done)
- **Trend and Reentry Sanity Weight Adjustments**:
  - Raised 15m asset trend: `WEIGHT_TREND_15M = 2.5` to strongly penalize contrary trend wiggles.
  - Raised DRT trend alignment sanity: `WEIGHT_SANITY = 2.0` to enforce trend matching.
  - Raised macro bias: `WEIGHT_HTF_BIAS = 2.0` to reject entries against the higher timeframe bias.
- **Accelerated Compounding Exit Scaling**:
  - Increased `"tp1_qty_ratio"` parameter inside all three sweeps strategies to `0.7` (70%). Exiting 70% of position size at TP1 (+1.5 RRR) secures massive compounding progress early, protecting realized profits.
