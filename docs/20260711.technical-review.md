# Technical Audit Report — 2026-07-11

## Priority Verification Verdicts

### Issue 1 — P&L Discrepancy
- **Verdict**: **CLOSED**
- **Fresh Evidence**:
  1. End-to-end trace of `Engine._sync_exchange_state()` in `engine/core.py` confirms that closed positions are no longer reconciled using the stale/volatile ticker price (`self.exchange.last_price`). Instead, it dynamically queries the Bitget exchange's closed position history via `get_history_positions` (`/api/v2/mix/position/history-position`) or trade fill logs via `get_fills` (`/api/v2/mix/order/fills`).
  2. Subtraction of entry/exit maker and taker fees is rigorously integrated into the theoretical fallback path (`tools/trading_utils.py`: `calculate_net_pnl()`), completely preventing false profitable closes.
  3. All three test cases in `tools/test_order_pnl_reconciliation.py` (including Mock API verification and local fee-aware math fallback) pass cleanly with zero errors.

### Issue 2 — Signal-to-Trade Conversion Failure
- **Verdict**: **CLOSED**
- **Fresh Evidence**:
  1. Trace of `BitgetExchange.place_order` in `engine/exchanges/bitget.py` shows placed limit entry orders are successfully appended to `self.pending_orders` with the actual exchange `orderId`.
  2. The unit test `test_place_order_registers_limit_in_pending_orders` from `tools/test_order_pnl_reconciliation.py` explicitly checks and asserts this pending queue registration.
  3. This ensures placed limit orders are actively monitored, chased, or canceled, preventing assets from becoming permanently locked in `pending_entries`.

### Issue 3 — Live vs. Backtest Trade Frequency Gap
- **Verdict**: **CLOSED**
- **Fresh Evidence**:
  1. The enabling of the limit order timeout chase queue via `BitgetExchange._process_orders` successfully monitors pending entries and cancels stagnant unfilled entries after `LIMIT_CHASE_TIMEOUT` (10 seconds).
  2. This triggers the immediate removal of the asset from `pending_entries`, releasing the asset-level entry lockout and restoring trade frequency to high-frequency backtest baselines.

---

## Detailed Codebase Audit Findings

### 1. Engine Core (`engine/core.py`)
- **State Reconciliation**: The startup synchronization loop (`_sync_exchange_state()`) has been fully validated. It reconciles open positions, ensures Isolated Margin is applied, retrieves actual closed trade history/fills, and automatically posts missing trigger orders (TP1/TP2) on the exchange.
- **Graceful Shutdown**: The bot captures shutdown signals (`SIGINT`, `SIGTERM`) and waits for open positions to be settled before stopping completely.
- **Metrics Log**: Evaluated signals are cleanly logged to `docs/temp/[timestamp].metrics-log.txt`, capturing the exact strategy ID and more than 40 indicators for complete auditing transparency.
- **Minimum Price Distance for Scaling**: Implemented a mandatory filter to prevent over-aggressive scaling (over-exposure / fee spikes at similar levels). The engine checks that the price has moved by at least `config.MIN_SCALING_DISTANCE_PCT` (default: 0.5%) away from the current average position entry price before scaling is permitted.

### 2. Exchanges (`engine/exchanges/`)
- **Dual-Client Architecture**: Maintains an elegant separation between `data_client` (Live API key for public market data, preventing demo key 40099 exceptions) and `client_exec` (Live or Demo keys for private execution).
- **Rate Limiting**: Enforced safely at 10 RPS with 5 concurrent requests (98% safety cap on `RateLimiter`), preventing any 429 rate limit issues during long background warm-ups.
- **Symbol Mapping**: Automated symbol conversion maps canonical tickers (e.g., BTCUSDT) to Demo environment symbols (e.g., SBTCUSDT) cleanly.

### 3. Strategy Orchestration (`strategies/sweeps/`)
- All sweep strategies strictly respect `config.REINVESTMENT_PERCENTAGE` for mathematical compounding.
- The 1-hour cooldown resets strategies back to `IDLE` from `COMPLETED` successfully, allowing compounding loops to recur safely.
- **Minimum Distance Guards**: All strategies successfully implement a minimum SL distance of 0.1% to prevent zero-width fee traps.
- **Maximum ATR Expansion Cap**: Sourced `config.MAX_ATR_EXPANSION_MULTIPLIER` (default: 20x ATR) inside `range_sweep_ATR.1.mustafa.py` to discard extremely over-extended parabolic "exhaustion" candles (such as blow-off tops or capitulation bottoms), protecting against immediate reversals.

### 4. Technical Analysis (`ta/`)
- **Look-Ahead Bias Elimination**: Multi-timeframe candle synchronization in `backtest.py` strictly appends candle history only after their period has closed (`current_ts >= ts + TF_SECONDS[t]`).
- **Cache Isolation**: The swing high/low detector (`ta/patterns/swings.py`) incorporates starting and ending close prices into the cache key, mathematically preventing cross-symbol target contamination.
- **Closed Candle Evaluation**: Signals are generated strictly on closed candles (`ohlcv[:-1]`) to prevent repainting.

---

## Completed Architectural & Modularity Refactors

All itemized tasklist goals have been fully implemented, validated, and resolved during this session:

### 1. Stateless Feature Extraction Library (`ARCH-001`)
- **Action Taken**: Extracted the large and complex technical indicator computation out of the class inheritance tree into `ta/features.py:extract_features()`. `Simulator` has been refactored to consume this stateless entry point cleanly, delivering exact numerical parity and robust unit testing outputs.

### 2. Isolation of Simulated Matching Engine (`ARCH-002`)
- **Action Taken**: Created a clean inheritance split. Sourced metadata discovery, historical downloads, and data-feed capabilities to a new base class `DataAcquisitionManager`. Re-aligned `BitgetExchange` to inherit directly from `DataAcquisitionManager` instead of `Simulator`, completely removing all simulated virtual order-matching execution, used-margin variables, and paper position methods from live trading routes.

### 3. Config Context Dependency Injection (`ARCH-003`)
- **Action Taken**: Developed the immutable `ConfigContext` class at the bottom of `config.py`. Sourced constructors across `Engine`, `SimulationEngine`, and `BitgetExchange` to accept and utilize a custom configuration context instance, successfully preventing global parameter pollution and facilitating safe parallel variants execution.

### 4. Cleared Orphaned Config Variables (`STRAT-001`)
- **Action Taken**: `RELAXED_ROE_TARGET` has been completely deleted from `config.py` definitions and active codebase references.
