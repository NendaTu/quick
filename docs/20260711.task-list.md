# Itemized Task List — 2026-07-11

## Architecture & Modularity Action Items

### Task ARCH-001: Decouple Feature Extractor into stateless module
- **ID**: `ARCH-001`
- **Description**: Refactor `Simulator.get_features` out of `simulator.py` into a pure stateless module (`ta/features.py`), allowing any class (Simulator, Backtest, Engine) to extract technical features from an OHLCV dict without inheriting or depending on the matching engine.
- **Source**: Modularity Findings Section 1
- **Verification criteria**:
  - `ta/features.py` compiles and exports `get_features(ohlcv, book, ...)`.
  - All unit tests in `tools/test_order_pnl_reconciliation.py` pass without regression.
- **Status**: Not Started
- **Dependencies**: None

### Task ARCH-002: Extract Simulator Matching Engine
- **ID**: `ARCH-002`
- **Description**: Extract the simulated order matching, trade execution, and position tracking logic from `Simulator` in `simulator.py` into a separate class (`SimulatedMatchingEngine` or `PaperExchange`), so that `BitgetExchange` is a pure adapter inheriting only from `BaseExchange`.
- **Source**: Modularity Findings Section 1
- **Verification criteria**:
  - `BitgetExchange` no longer inherits from `Simulator`.
  - `BitgetExchange` handles its own historical candle downloads cleanly.
- **Status**: Not Started
- **Dependencies**: ARCH-001

### Task ARCH-003: Refactor config.py into Immutable Context
- **ID**: `ARCH-003`
- **Description**: Load all settings in `config.py` into an immutable `ConfigContext` data class during startup, passing this context cleanly to `Engine`, `Simulator`, and strategy class constructors to eliminate reliance on re-importing global state.
- **Source**: Modularity Findings Section 2
- **Verification criteria**:
  - Global `config` imports are removed from active modules.
  - Multi-asset portfolio backtests pass correctly without modifying global state.
- **Status**: Not Started
- **Dependencies**: None

---

## Strategy & Performance Audit Action Items

### Task STRAT-001: Delete Orphaned Config Variables
- **ID**: `STRAT-001`
- **Description**: Remove `RELAXED_ROE_TARGET` from `config.py` and clean up any remaining references in archived strategy documentation or comments to keep configuration scope clear.
- **Source**: Variable Trace Report
- **Verification criteria**:
  - `RELAXED_ROE_TARGET` is completely removed from `config.py`.
  - `grep -rn "RELAXED_ROE_TARGET" .` returns no active code matches.
- **Status**: Not Started
- **Dependencies**: None
