# Session Focus & Decisions - 2026-06-25

This document tracks the specific interests, decisions, and statuses for the current development session. Content in this document is never deleted, only updated or added to.

## 1. Decisions and Assumptions (Established at Session Start)

- **Stop Loss Execution**:
    - *Decision*: Maintain the current **Soft-Stop** logic (Limit SL with Market backup buffer).
    - *Metric*: Monitor performance of Limit fills vs. Market backup fills.
    - *Status*: Active.

- **RSI Ceiling for Sells**:
    - *Decision*: Keep the current `RSI_SHORT_CEILING = 80.0` as a baseline.
    - *Metric*: Analyze if this ceiling prevents entering profitable reversals or avoids "moon" traps.
    - *Status*: Active.

- **ATR-Based Stop Loss**:
    - *Decision*: Test and validate the existing ATR-based SL implementation before considering logic changes.
    - *Metric*: Observe if ATR-based stops improve survival during high-volatility periods compared to fixed % stops.
    - *Status*: Active.

- **TP/SL Barrier Testing**:
    - *Decision*: Set the next baseline at **0.8% TP / 0.4% SL** (1:2 Risk/Reward Ratio).
    - *Metric*: Evaluate Win Rate and Net Profitability at this wider barrier.
    - *Status*: Completed (Run 32). Win Rate 63.6%, PnL -4.76 USDT.

- **Concurrency Caps**:
    - *Decision*: Maintain `MAX_CONCURRENT_POSITIONS = 50`.
    - *Status*: Active.

- **Dynamic TP Relaxation**:
    - *Decision*: Stick to a static **5% TARGET_NET_ROE** until more data is available.
    - *Status*: Active.

- **Optimized Breakeven Trigger**:
    - *Decision*: Implement profit buffer (Fees + 1% ROE) to ensure protected trades contribute to PnL.
    - *Status*: Implemented & Verified (Run 33).

- **Global Momentum Gate**:
    - *Decision*: Implement BTC 15m momentum filter to prevent fighting macro trends.
    - *Status*: Implemented & Verified (Run 34).

- **Fee-Aware Sizing**:
    - *Decision*: Factor round-trip fees into risk-per-unit calculation.
    - *Status*: Implemented & Verified (Run 35).

- **Persistent Asset Discovery**:
    - *Decision*: Cache discovered assets in DB for 6 hours to reduce warm-up overhead and API calls.
    - *Status*: Implemented.

## 2. Bug Fixes

- **Inverted Soft-Stop Logic**:
    - *Impact*: Caused all Long positions to immediately exit via "STOP_BACKUP" because the disaster distance calculation incorrectly used Short position logic.
    - *Fix*: Swapped the `side == "buy"` and `side == "sell"` logic in `simulator.py` to correctly calculate distance based on the position direction (Longs exit on crash, Shorts exit on moon).
    - *Status*: Fixed.

## 3. Environment Setup
- **API Access**: Bitget paper mode credentials configured in `.env`.
- **Initial Capital**: 1000 USDT at 0.2% risk per trade.
