# Session Focus - 2026-07-07

## Goals
- Get Bitget Demo Mode fully designed, implemented, integrated, and tested.
- Ensure it works alongside `main.py`, `compare.py`, and `backtest.py` (where applicable).
- Implement real Bitget V2 order placement with SL/TP support.

## Phase 1: Audit and Baseline
### Current Findings
- `MODE` in `config.py` defaults to `paper`.
- `Engine` handles `paper` by using `SimulationEngine`.
- `Engine` treats other modes as `BitgetExchange` but with a warning.
- `SignalRouter` has some branching for `demo` but it's incomplete.
- `BitgetExchange` has stubs for `place_order`.
- `BitGetClient` uses `https://api.bitget.com` as base URL.

### Broken/Blockers identified
- [ ] Implement real Bitget V2 order placement.
- [ ] Refactor `Engine` to support `demo` and `live` distinctly.
- [ ] Ensure `BitGetClient` can switch to Demo endpoints.

## Questions & Decisions
- **Decision**: Introduce `BITGET_API_KEY_DEMO`, `BITGET_SECRET_KEY_DEMO`, and `BITGET_PASSPHRASE_DEMO`.
- **Decision**: `BitGetClient` will take a `base_url` or a `is_demo` flag.
- **Decision**: Implement real order placement with `limit` orders, embedded SL/TP.
