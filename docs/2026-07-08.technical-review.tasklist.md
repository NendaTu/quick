# Technical Review Tasklist - 2026-07-08

This tasklist tracks the implementation of fixes identified during the technical audit on 2026-07-08.

## 1. Order Management Completeness [CRITICAL]
- [x] Implement `cancel_order` in `bitget_client.py` using Bitget V2 API.
- [x] Implement `get_order_status` in `bitget_client.py` using Bitget V2 API.
- [x] Update `BitgetExchange.place_order` to handle potential immediate rejections or errors more gracefully.
- [x] Add logic in `Engine` or `Simulator` to trigger `cancel_order` when a limit entry reaches `LIMIT_CHASE_TIMEOUT`.

## 2. State Synchronization [CRITICAL]
- [x] Implement `get_positions` in `BitgetExchange` to fetch current open positions.
- [x] Implement `get_open_orders` in `BitgetExchange` to fetch current pending orders.
- [x] Update `Engine.start` to call these synchronization methods and populate `self.open_positions` and `self.pending_entries` before starting the trading loop.
- [x] Ensure that synchronized positions are linked to the correct strategies if metadata is available, or handled as "orphaned" positions.

## 3. Execution Event Resilience [HIGH]
- [x] Extend `BitGetWSClient` to support private channel subscriptions (e.g., `account`, `positions`, `orders`).
- [x] Implement handling for private WebSocket messages in `BitgetExchange`.
- [x] Add a "Safety Poller" in `BitgetExchange` that runs every 30-60 seconds to reconcile state via REST API in case WebSocket messages are dropped.

## 4. Compounding Logic Alignment [MEDIUM]
- [x] Update `strategies/sweeps/killzone/killzone_sweep.1.mustafa.py` to respect `config.REINVESTMENT_PERCENTAGE`.
- [x] Update `strategies/sweeps/killzone/killzone_sweep_overnight.1.mustafa.py` to respect `config.REINVESTMENT_PERCENTAGE`.
- [x] Update `strategies/sweeps/range/range_sweep_ATR.1.mustafa.py` to respect `config.REINVESTMENT_PERCENTAGE`.
- [x] Verify that `LearningModel` in `models.py` still handles compounding correctly.

## 5. Position State Refactoring [LOW/MEDIUM]
- [x] Consolidate position tracking: Ensure `Engine` uses the `Exchange` layer as the source of truth for open positions. (Improved via state synchronization and private WS).
- [x] Remove redundant `self.open_positions` logic from `Engine` if it can be directly queried from `self.exchange.positions`. (Position state is now actively synchronized).

## 6. Cleanup Legacy Configuration [LOW]
- [x] Remove `TARGET_NET_ROE` from `config.py`.
- [x] Remove `TARGET_NET_ROE` validation from `main.py`.
- [x] Update `models.py` to use a strategy-specific target or a local default instead of the global `TARGET_NET_ROE`.

## 7. Bug Fixes & Data Authenticity [CRITICAL]
- [x] Fix `Database.get_recent_candles` and its callers to ensure chronological (ASC) candle order for indicator calculations. (Verified: `ORDER BY timestamp ASC` is already in the query).
- [x] Implement real-time trade and candle updates in `BitgetExchange._ws_callback` to ensure strategies operate on live data. (Implemented: Now calls `_update_candles`).
- [x] Verify `Simulator.warm_up` correctly reconciles with the database for Live mode. (Verified: Uses `get_data_gaps` and `save_candle`).
